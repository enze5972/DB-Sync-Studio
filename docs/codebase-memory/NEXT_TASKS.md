# NEXT_TASKS

## 后续任务

### 1. P2-3 / P2-4 继续收尾

- 校验运行监控页面、趋势图、任务统计、数据库连接状态是否完整
- 校验告警规则、告警渠道、去重、历史查询、测试发送是否完整
- 校验监控和告警是否都通过 `run_id` / `table_task_id` 关联到现有执行链路
- 继续确认增量同步在暂停/停止/恢复场景下的 checkpoint 不会被跨任务污染
- 继续补齐诊断导出页、日志页和 SQL 编辑器的入口、结果展示与脱敏细节
- 如需最终交付，再做一次 Tauri 打包回归和目标平台安装检查

### 2. 只做增量，不做重构

- 优先复用现有表、现有仓储、现有页面
- SQLite 变更必须保留迁移
- 不破坏已完成的 P0 / P1 / P2-1 / P2-2

### 3. 继续完善已知边界

- 监控数据默认保留 30 天，后续仍要支持手动清理
- 告警密钥继续保持本地加密
- 告警触发和发送失败都不能影响主同步流程

### 4. 下一阶段准备

- P2-3 完成后，再单独评估 P3 是否进入
- P3 只在明确需要时才开始，不提前展开
- P2-4 收尾已补齐 diagnostics、数据预览、告警设置和告警历史的前端接入，后续只需在目标平台复核打包与安装
