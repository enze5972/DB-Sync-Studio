# TASK_TEMPLATE

可直接复制后填写，用于单个开发任务。

## 任务名称

`<填写任务名称>`

## 任务目标

`<填写本次任务要完成的具体目标>`

## 允许读取范围

- 先读 `AGENTS.md`
- 先读 `docs/codebase-memory/WORKFLOW.md`
- 先读与当前任务直接相关的 module memory
- 只读与当前任务直接相关的源码文件
- 源码初始读取最多 5 个文件
- 优先使用搜索定位，再打开命中的文件

## 禁止读取范围

- 禁止默认全仓库扫描
- 禁止读取与当前任务无关的目录
- 禁止为了“理解全貌”打开整个模块目录
- 禁止重复读取无变化文件

## 修改限制

- 不修改业务代码，除非任务明确要求
- 只做最小正确变更
- 不做无关重构
- 不逐文件复制代码
- 不输出完整代码文件

## 输出限制

- 不要长篇解释或过程性分析
- 回复保持简短
- 默认只输出任务结果，不展开背景

## 测试 / 构建要求

- `<填写需要执行的测试或构建命令>`
- `<填写验证通过的标准>`
- 如无法执行，必须说明原因

## memory 更新要求

- 完成一个阶段后更新相关 `docs/codebase-memory` 文件
- 优先更新 `CONTEXT_COMPACT.md`
- 必要时更新 `IMPLEMENTATION_STATUS.md`
- 必要时更新对应模块 memory

## 执行提示

- 先搜索，再打开文件
- 5 个源码文件不够时先暂停说明
- 不确定时不要猜
- 以最小 token 完成任务
